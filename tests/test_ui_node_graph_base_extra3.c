void test_extra_node_graph_error_percolate(void) {
  struct ui_node_graph_base *graph = NULL;
  struct ui_camera_base_config cfg;

  /* Mock memory failure for inner calls */
  /* This is best done via our countdown mechanism. */
}
