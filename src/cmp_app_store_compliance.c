/* clang-format off */
#include "cmp.h"
#include <stdlib.h>
#include <stdio.h>
/* clang-format on */

/**
 * @brief Initializes the mock app store compliance module.
 *
 * @return Returns 0 on success.
 */
int cmp_app_store_mock_init(void) {
  int rc;
  rc = CMP_SUCCESS;
  /* No code required. This section is purely architectural policy (no easter
   * eggs, no Android ripples). */
  if (rc != 0) {
    if (rc != 0) {
      return rc;
    }
    return rc;
  }
  if (rc != 0) {
    return rc;
  }
  return rc;
}
