# LibCMPC Examples

This directory will hold complete source code snippets and example apps specifically designed to be embedded into the Doxygen documentation via the `\include` or `\example` tags. 

*Currently empty. Check `cmp_demo/` for the main application demo.*
